# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.next
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
test-results/
playwright-report/
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Production deployment (added with the production setup) ──
# The existing `*.local` pattern catches .env.local but nothing else, so the
# real env filenames are listed explicitly.
.env
.env.production
.env.production.local
.env.staging
!.env.example

# TLS material and private keys.
*.pem
*.key
*.p12
*.pfx

# Database dumps and backups.
backups/
*.sql.gz
*.dump
